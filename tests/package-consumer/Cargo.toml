[package]
name = "relaygate-package-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
relaygate-destination = "=0.5.5"
relaygate-protocol = "=0.5.5"
relaygate-sdk = "=0.5.5"
relaygate-token-issuer = "=0.5.5"
relaygate-transport = "=0.5.5"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
